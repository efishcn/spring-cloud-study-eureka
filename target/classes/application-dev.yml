server:
  port: 8899
spring:
  application:
    name: EUREKA-CENTER
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8899/eureka/